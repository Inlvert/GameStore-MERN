const SERVER_URL = 'game-store-mern.vercel.app/';

const CONSTANTS = {
  PUBLIC_URL: "http://localhost:3000/",
  HTTP_SERVER_URL: `https://${SERVER_URL}`,
  REFRESH_TOKEN: "refreshToken",
};

export default CONSTANTS;
